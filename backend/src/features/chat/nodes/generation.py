from backend.src.features.chat.state import QueryGraphState


def generate_initial_response(state: QueryGraphState) -> str:
    return "This is the initial response based on the query and conversation history."
